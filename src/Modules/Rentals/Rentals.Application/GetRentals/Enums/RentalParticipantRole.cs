namespace Rentals.Application.GetRentals.Enums;

/// <summary>
/// Represents the role of the authenticated user in a rental.
/// / Representa o papel do usuário autenticado em um aluguel.
/// </summary>
public enum RentalParticipantRole
{
    Provider = 1,
    
    Renter = 2
}